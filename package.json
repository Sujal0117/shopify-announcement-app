{
  "name": "shopify-announcement-app",
  "version": "1.0.0",
  "description": "Shopify Announcement App",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "build": "cd web && npm install --legacy-peer-deps && cd ../client && npm install && npm run build",
    "start": "cd web && node index.js"
  },
  "engines": {
    "node": "18.x"
  }
}
