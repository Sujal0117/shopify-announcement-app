[build]
builder = "nixpacks"
buildCommand = "cd web && npm install"

[deploy]
startCommand = "cd web && node index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 30
